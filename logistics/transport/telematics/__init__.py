"""DEPRECATED: this package has moved to ``goconnect.land``.

Provider integration and the ingest tick are now part of the GoConnect app.
The modules here remain as thin shims that re-export from
``goconnect.land.*`` so Transport Vehicle / Run Sheet integrations and the
debug APIs in ``logistics`` keep working unchanged.

Removal of these shims is tracked separately; do not add new code here.
"""

from __future__ import unicode_literals
