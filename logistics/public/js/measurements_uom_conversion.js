// Stub for Measurements UOM Conversion doctype client script (avoids 404 when script is requested)
// Add form handlers here if this doctype is used in the app.
frappe.provide('frappe.ui.form');
if (typeof frappe.ui.form.on !== 'undefined') {
	frappe.ui.form.on('Measurements UOM Conversion', {});
}
